---
title: Spring 프레임워크 구조
date: 2026-03-13-00:30
categories:
- Spring
tags:
- Spring
- Framework
---

> "**스프링 프레임워크는 경량 컨테이너로 자바 객체를 담고 직접 관리한다.**"

---

## 목표

> 스프링 프레임워크의 기본 구조와 규칙을 알아봅니다.

---

## 구성 요소

### ServletContainer(서블릿 컨테이너)

- 클라이언트로부터 `HTTP` 요청을 받아 서블릿 로직을 처리할 수 있는 구성요소이다.
- 원래 `HTTP` 요청을 받으면 개발자는 서버 소켓 열기, `HTTP` 요청 파싱, 스레드 관리, 요청/응답 객체 생성, 서블릿 실행, 응답 전송을 구성해야한다.
- 이걸 서블릿 컨테이너가 대신하고 개발자는 비즈니스 로직만 작성할 수 있게 해준다.
- 대표적인 예로 Tomcat이 있다.

### Spring(스프링)

- 자바로 웹 애플리케이션을 쉽게 만들도록 도와주는 애플리케이션 프레임워크이다.
- 객체 관리(IoC) + 의존성 주입(DI) + 웹 MVC 구조를 제공준다.
- 보통 객체를 싱글톤으로 생성한다.

#### 여기서 중요한 건, 객체마다 stateless 이기에 connection pool을 통해 DB에 반영하고, 데이터를 처리하기에 싱글톤으로 Bean이 생성된다.

- DB 반영은 동시에 요청이 오면, Tomcat이 요청마다 스레드를 하나씩 할당하고 Spring은 ThreadLocal 구조를 사용하기에 겹칠 수 없다.
- 다만, 같은 DB 데이터를 건드릴 경우 DB의 트랜잭션과 락으로 해결한다.

### DispatcherServlet(디스패처 서블릿)

- 모든 `HTTP` 요청을 한 곳에 모아 알맞은 컨트롤러로 전달하는 중앙 관리자이다.
- `HandlerMapping`, `HandlerAdapter`을 통해 컨트롤러에 요청을 전달하고 실행 후, JSON 형태로 반환한다.

### Controller(컨트롤러)

- 개발자가 직접 개발하는 컴포넌트로, Http Request를 개발자가 처리할 수 있도록 개발하는 컴포넌트이다.

### Service(서비스)

- 개발자가 직접 개발하는 컴포넌트로, 비즈니스 로직을 수행하는 컴포넌트이다.

### DAO(Data Access Object = Repository)

- 개발자가 직접 개발하는 컴포넌트로, DB에 직접적으로 접근하는 객체이다.

### ViewResolver(뷰 리졸버)

- ViewName을 기반으로 어떤 View 파일을 사용할 것인지 확인해주는 컴포넌트이다.
- 예전 서버가 HTML 화면을 직접 만들어서 브라우저로 보내는 구조라, 요즘은 잘 사용되지 않는다.

### View(뷰)

- 개발자가 직접 개발하는 컴포넌트로, 보통 UI 화면을 말한다.
- 요즘 프론트엔드는 React, Next.js 등을 사용하기에 View 구조가 없다. 바로 JSON을 반환하여 프론트에게 전달한다.

---

## Reference

- https://devscb.tistory.com/119#google_vignette